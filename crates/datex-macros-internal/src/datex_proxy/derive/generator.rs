use proc_macro2::TokenStream;
use crate::datex_proxy::data::StructureData;

/// Generates the code for the derive macro based on the provided structure data.
pub fn generate_derive_code(
    structure_data: StructureData,
) -> TokenStream {
    todo!()
}